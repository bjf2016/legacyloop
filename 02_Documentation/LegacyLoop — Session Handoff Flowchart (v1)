┌──────────────────────────┐
│  Command Center Chat     │
│  (Strategic / Overview)  │
└───────────┬──────────────┘
            │
            ▼
     ┌───────────────┐
     │ Start Session │
     │   N (new chat)│
     └───────────────┘
            │
            │  Paste kickoff block from latest
            │  Continuation doc
            ▼
┌──────────────────────────────┐
│  Execute Technical Steps     │
│  (1 step at a time, Laura L) │
└──────────────┬───────────────┘
               │
               │  When final step done ✅
               ▼
     ┌────────────────────────────┐
     │  END OF SESSION N (Manual) │
     └────────────────────────────┘
               │
               │  ↓ Manual checks
               │  1️⃣ Verify all tasks done
               │  2️⃣ Update any screenshots
               │  3️⃣ Commit code changes (if any)
               │
               ▼
     ┌───────────────────────────────┐
     │  Run `end_session.ps1` Script │
     └───────────────────────────────┘
               │
               │  🔧 Script actions:
               │  - Generate Continuation doc (N→N+1)
               │  - Update SprintTracker.md
               │  - Update Progress_Log.md
               │  - Update command_center_state.json
               │  - Commit & push to GitHub
               │
               ▼
┌────────────────────────────────────────┐
│ Confirm in VS Code:                    │
│ • 01_Project_Plan/Continuations updated│
│ • SprintTracker shows N done, N+1 next │
│ • command_center_state.json refreshed  │
└────────────────────────────────────────┘
               │
               │
               ▼
     ┌───────────────────────────────┐
     │ Return to Command Center Chat │
     └───────────────────────────────┘
               │
               │ Upload or mention:
               │ • Continuation doc
               │ • command_center_state.json
               │ (I’ll verify & prepare next kickoff)
               │
               ▼
┌──────────────────────────┐
│ Start Session N+1        │
│ (new chat or continuation│
│  depending on complexity)│
└──────────────────────────┘

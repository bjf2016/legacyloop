# LegacyLoop — FatherCast
**Session 1: Environment, Git, and Baseline Setup**  
**Date:** 2025-11-08  
**Developer:** Ben Foroo  
**Persona:** Laura L — one step at a time, confirm after each step  
**PRD:** v1.3 (Locked for Development)  
**Repos:** `legacyloop` (docs) and `legacyloop-app` (Next.js app)  
**Stack:** Next.js 14 · Supabase (US-West) · OpenAI (Whisper + GPT-4o-mini) · Vercel · PWA

---

## Where We Left Off
- PRD v1.3 (READABLE + DEV) finalized and saved.
- PRD_Changelog updated to include v1.3.
- Continuation v1.3 template created.

## Goal of Session 1
1) Verify local tooling in VS Code (with ChatGPT/Codex extension).  
2) Initialize a **docs** Git repo and push to GitHub (`legacyloop`).  
3) Scaffold a **Next.js app** repo and push to GitHub (`legacyloop-app`).  
4) Connect `legacyloop-app` to Vercel; create Supabase project (US-West); prep env vars.

---

## Single Next Step (to execute now)
**Step 1 — Open the project and pin the PRDs in VS Code**

**Action**
- Open folder: `C:\DEV\LegacyLoop_Project`
- In the Explorer, pin (keep open):
  - `00_PRD\LegacyLoop_PRD_v1.3_READABLE.md`
  - `00_PRD\LegacyLoop_PRD_v1.3_DEV.md`
  - `00_PRD\PRD_Changelog.md`

**Expected Outcome**
- Workspace shows the three PRD files open in tabs.
- You can quickly reference them during setup.

**Verification**
- Screenshot or simple confirmation: “Done Step 1”

---

## Upcoming Steps (don’t execute yet)
- **Step 2:** Install/verify VS Code ChatGPT (Codex) extension & sign in.  
- **Step 3:** Verify Node, npm, Git; fix PowerShell execution policy if needed.  
- **Step 4:** Initialize Git for **docs** repo and push to GitHub (`legacyloop`).  
- **Step 5:** Scaffold Next.js app (`legacyloop-app`), init Git, push to GitHub.  
- **Step 6:** Connect Vercel to `legacyloop-app`, set env vars.  
- **Step 7:** Create Supabase (US-West), link CLI, `db push`.  
- **Step 8:** Create `.env.local` and run `npm run dev` locally.

---

## End-of-Session Checklist
- [ ] Docs repo on GitHub with PRDs committed
- [ ] App repo on GitHub with baseline Next.js scaffold
- [ ] Vercel project linked to `legacyloop-app`
- [ ] Supabase project created; CLI linked; migrations planned
- [ ] `.env.local` template created in the app
